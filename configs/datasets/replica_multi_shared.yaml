dataset:
  name: replica
  phase: A
  root: /mnt/3dgs-ssd/3dgs-stage/priorprobe3dgs/replica_colmap_multi
  format: colmap
  default_scene_id: room_0
  source_path_template: "{root}/{scene_id}"
  images: images
  depths: ""
  eval: true
  white_background: false
  scene_type: controlled_indoor_multi_object
  object_count_range: [2, 4]
  notes:
    - Replica multi-object oracle exports are staged on SSD for actual training runs.
    - Scene subsets are rendered with scene-centric camera paths for top-K supported objects.
  scenes:
    room_0:
      relative_path: room_0
    office_0:
      relative_path: office_0
