experiment:
  name: domain_incremental
  initialization: prior
  dataset_config: configs/datasets/custom_capture.yaml
  prior_config: configs/priors/shapesplat.yaml
  retrieval_mode: automatic
  alignment_mode: automatic
  adaptation: incremental_only
  prior_object_id: lamp_domain

trainer:
  max_steps: 12000
  target_quality:
    metric: psnr
    value: 29.0
