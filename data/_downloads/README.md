# Raw Downloads

원본 ScanNet/Replica 다운로드는 이 아래에 둔다. 이 레포는 다운로드를 자동화하지 않으며, 라이선스/접근 권한도 여기서 처리하지 않는다.

## Expected Roots

- `data/_downloads/replica_dataset/`
- `data/_downloads/scannet/`

원본 데이터는 여기서 보관만 하고, 실제 학습에는 `data/public_datasets/*_colmap/<scene_id>/`로 export한 subset scene을 사용한다.
